import React from 'react';
import s from "./style.module.scss"
const Hero = () => {
    return (
        <div className={s.container}>
            blah blah
        </div>
    );
};

export default Hero;
